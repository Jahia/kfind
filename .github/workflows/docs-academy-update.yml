# DO NOT EDIT
# This file is managed globally at https://github.com/Jahia/.github
name: Academy Docs - Update

on:
  workflow_dispatch:
  pull_request:
    types: [closed]
    paths:
      - "docs/**"

jobs:
  WF:
    uses: Jahia/jahia-modules-action/.github/workflows/reusable-academy-update.yml@v2
    secrets: inherit
